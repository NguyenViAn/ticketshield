name: CI

on:
  push:
    branches:
      - main
      - master
  pull_request:

jobs:
  verify:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: ticketshield/package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: ticketshield

      - name: Lint
        run: npm run lint
        working-directory: ticketshield

      - name: Typecheck
        run: npm run typecheck
        working-directory: ticketshield

      - name: Build
        run: npm run build
        working-directory: ticketshield
